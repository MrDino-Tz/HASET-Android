<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Duplicate BottomNavText moved to styles_bottom_nav.xml -->

    <!-- Custom style for BottomSheetDialog with animations -->
    <style name="BottomSheetDialogAnimation" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowAnimationStyle">@style/BottomSheetAnimation</item>
    </style>

    <style name="BottomSheetAnimation">
        <item name="android:windowEnterAnimation">@anim/anim_slide_up</item>
        <item name="android:windowExitAnimation">@anim/anim_slide_down</item>
    </style>

    <!-- ButtonPrimary and EditTextStyle moved to themes.xml to avoid duplicates -->




</resources>
